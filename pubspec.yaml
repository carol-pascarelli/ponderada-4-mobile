name: ponderada_4_mobile
description: Reminders - app de tarefas, Pomodoro e plano de estudos com IA.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  provider: ^6.1.4
  http: ^1.3.0
  flutter_local_notifications: ^19.0.0
  timezone: ^0.10.0
  share_plus: ^10.1.4
  geolocator: ^13.0.4
  geocoding: ^3.0.0
  url_launcher: ^6.3.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
